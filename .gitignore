# CMake build trees
build/
build-*/

# Compile database (regenerated by cmake on every configure)
compile_commands.json

# clangd / LSP caches
.cache/

# Tool-local session state
.opencode-session

# Core dumps
core
core.*
vgcore.*

# Editor / OS cruft
*.swp
*.swo
.DS_Store
